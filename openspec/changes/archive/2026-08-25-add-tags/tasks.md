## 1. データ層

- [x] 1.1 タグの表と、メモとタグの対応の表を定義する（design.md D1・D2）。同じ利用者の中で名前は一意。**対応の表は多対多のまま**にし、1メモ1タグを表の形で表現しないこと。検証: マイグレーションを生成してローカル D1 に適用し、同じ名前を2度作れないこと、および `memo_tags` の主キーが `memo_id` 単独になっていないことを確認する
- [x] 1.2 タグの付与・取り外しを実装する。**1メモ1タグの上限を `MAX_TAGS_PER_MEMO` として1箇所に置き**、付与の経路をすべてそこに通す（design.md D2）。検証: 同じタグを重ねて付けても1つのまま、別のタグを付けると差し替わり、外すと紐づけだけが消えてタグ自体は残ることをテストで確認する
- [x] 1.3 **上限を緩めるのが1箇所で済むことを確かめる。** 検証: `MAX_TAGS_PER_MEMO` を 2 にすると1件のメモが2つのタグを持てるようになり、スキーマもマイグレーションも触らずに済むことをテストで確認する（確認後は 1 に戻す）
- [x] 1.4 名前の正規化と検証を実装する（前後の空白を無視、空を拒否、長さの上限）。検証: 空白だけの名前が拒否され、前後に空白のある名前が既存のタグと同じものとして扱われること
- [x] 1.5 タグが利用者ごとに分離されることをテストする。検証: 2人が同じ名前のタグを使っても互いのメモが混ざらず、他人のメモにタグを付けられないこと
- [x] 1.6 メモを削除したとき、タグとの紐づけも消えてタグ自体は残ることをテストする。使われなくなったタグも消さないこと

## 2. 絞り込み

- [x] 2.1 タグ1つでメモを絞り込む読み取りを実装する（design.md D5、サーバー側）。検証: そのタグを持つメモだけが返り、他の利用者のメモが混ざらないことをテストで確認する
- [x] 2.2 タグの一覧（名前と件数）を返す読み取りを実装する。検証: 自分のタグだけが、そのタグを持つ自分のメモの件数とともに返ること
- [x] 2.3 **絞り込みが復習に影響しないことをテストする。** 検証: 絞り込んだ状態でも出題対象が全件から選ばれること。**出題の取得にタグを渡す実装にすると落ちること**も確認する

## 3. 画面（タグ）

`docs/design-decisions.md` の語り口と配色に合わせる。下部タブは増やさない。

- [x] 3.1 メモ詳細のシートを作る（design.md D3）。タグの付け外しをここに置く。検証: 一覧からメモを選ぶと開き、タグを付けると一覧に反映されることをブラウザで確認する
- [x] 3.2 メモの削除を詳細へ移し、一覧の行から外す（design.md D4）。検証: 一覧に削除の導線が無く、詳細から削除でき、確認を取り消すと消えないことをブラウザで確認する
- [x] 3.3 一覧の各メモにタグを表示する。タグを持たないメモはそれと分かること。検証: 3行以上ある状態で、タグの有無が本文を読まずに見分けられること
- [x] 3.4 絞り込みの帯を一覧の上に置く（design.md D5）。検証: タグを選ぶと絞り込まれ、解除でき、0件のときも解除の手段が残ることをブラウザで確認する
- [x] 3.5 コントラストを実測する。検証: タグの表示・帯・詳細シートについて、ライト・ダーク両モードで文字 4.5:1 以上、非文字の部品 3:1 以上であることを計算値で示す

## 4. 提案の中身（外部に出ない部分）

change 5 の `lib/quiz-generation.ts` と同じ形にする。外部呼び出しと分ける。

- [x] 4.1 提案の入力を組み立てる関数を書く（design.md D8・D9）。未分類のメモと**既存のタグ名**を渡す。検証: 型チェックが通り、その関数が HTTP・D1・フレームワークを import していないこと
- [x] 4.2 1回に渡すメモに上限をかけることをテストする（design.md D7）。検証: 上限を超える未分類があっても、入力に含まれるのは上限までであること
- [x] 4.3 応答を検証する純粋関数を書く。検証: 空のタグ名・長すぎる名前・型違いが弾かれ、**自分が渡していないメモの id が捨てられる**ことをテストで確認する
- [x] 4.4 **同じメモに2つ以上のタグが提案されたとき、1つだけを採る**ことをテストする（spec の要件）
- [x] 4.5 他人のメモが入力に含まれないことをテストする

## 5. 提案の呼び出しと適用

- [x] 5.1 提案を呼ぶ関数を、呼び先を差し替えられる形で実装する。検証: 偽の呼び先で成功・失敗・壊れた応答の3通りを扱えることをテストで確認する。**ネットワークに出ないこと**
- [x] 5.2 **実際の HTTP 層をテストする。** 検証: fetch だけを偽物にして、経路・ヘッダ・本文が正しいこと。**ヘッダ名を間違えると落ちること**も確認する（change 5 のレビュー指摘 中-1 と同じ形を繰り返さない）
- [x] 5.3 提案の受け入れを実装する。検証: 提案どおりにタグが付き、知らないメモの id が混ざっていても無視されることをテストで確認する
- [x] 5.4 提案が失敗しても既存のタグが変わらないことをテストする
- [x] 5.5 鍵が無い環境では提案を示さないことをテストする（design.md D11）

## 6. 提案の契機と画面

- [x] 6.1 未分類の件数を数え、一定数に達したかを判定する純粋関数を書く（design.md D6・D10）。検証: 断ったときの件数を上回るまで真にならないことをテストで確認する
- [x] 6.2 提案の状態（最後に断ったときの件数）を持つ表を定義する。検証: 利用者ごとに1行で、断ると更新されることをテストで確認する
- [x] 6.3 一覧に提案の帯を出す。検証: 未分類がたまると出て、断ると消え、次にたまるまで出ないことをブラウザで確認する
- [x] 6.4 承認の画面を作る。**タグ名と件数だけ**を示す（design.md 制約3）。検証: どのメモに何が付くかが画面に出ていないこと
- [x] 6.5 受け入れると一覧に反映されることを確認する。検証: 付いたタグで絞り込めること
- [x] 6.6 提案の失敗が警告として現れないことを確認する（design.md D11）

## 7. デプロイ

- [x] 7.1 本番の D1 にマイグレーションを適用する。検証: 本番に3つの表があること
  - 2026-08-25 に適用。`tags` / `memo_tags` / `tag_suggestion_state` の3つを確認。`memo_tags` の主キーが `PRIMARY KEY(memo_id, tag_id)` の複合のままであることも本番で確認した（design.md D2 の前提）
- [x] 7.2 デプロイして、実機でタグを付けて絞り込めることを確認する
  - **2026-08-25 に確認できた。** 実機でタグを付けられ、絞り込みも動いた。本番の D1 にも紐づけが入っている。1度目の確認で欠陥が2件出ている（決定ボタンが無い／iOS が連絡先の自動入力を出す）。どちらも直して出し直した
- [x] 7.3 実機で提案を1回通す。検証: タグ名と件数が示され、受け入れると一覧に反映されること
  - **2026-08-25 に確認できた。** 本番の D1 に「用語 3件」「生活 2件」が入っており、提案が通って受け入れられたことが記録側からも裏付けられる
- [x] 7.4 `docs/deploy.md` に、提案が change 5 と同じ鍵を使うことを追記する。検証: 呼び出しが増える経路として提案が挙がっていること

## 8. 仕上げ

- [x] 8.1 spec に書かれたシナリオを一通り辿る
- [x] 8.2 `npx vitest run`、型チェック、`next build`、OpenNext ビルド、cron worker の型チェック、`scripts/scan-secrets.sh` がすべて通ること
- [ ] 8.3 実際に使ってみて、タグが乱立しないか、絞り込みが役に立つか、**1メモ1タグで足りるか**を見る（design.md Open Questions・Risks）。**このタスクは人間の判断が要る**
  - **未実施のままアーカイブした（2026-08-25）。** 日数が要るので change の完了条件から外す。change 5 の 8.4 と同じ扱い。判断の材料は3つ: タグが乱立しないか／絞り込みが役に立つか／1タグで足りるか（「仕事」と「読書」の両方に当てはまるメモが出てきたら、`MAX_TAGS_PER_MEMO` を上げる）
