## 1. 記録を残す

- [x] 1.1 想起の出来事の表を定義する（いつ・どの問答・思い出せたか）。問答が消えたら記録も消えること。検証: マイグレーションを適用し、問答を削除すると紐づく記録が消えることをテストで確認する
- [x] 1.2 採点したときに記録を残す。**既存のべき等性（`occurrenceAt` の比較）には触れない**（design.md D6）。検証: 採点すると記録が1件増え、次回出題日も更新されることをテストで確認する
- [x] 1.3 **同じ採点が二度届いても記録は1件のまま**であることをテストする。検証: 同じ `occurrenceAt` で二度採点し、記録が増えないこと
- [x] 1.4 思い出せなかった採点も記録に残ることをテストする
- [x] 1.5 メモを消すと記録も消えることをテストする。検証: メモ → 問答 → 記録の連鎖で消えること
- [x] 1.6 記録が利用者ごとに分離されることをテストする。検証: 2人分の採点を作り、他方の記録が混ざらないこと

## 2. 読み取り

- [x] 2.1 思い出せた回数の累計を数える読み取りを実装する。検証: 自分の記録だけを数えること、思い出せなかった回は含めないことをテストで確認する
- [x] 2.2 **累計が減らないことをテストする**（spec の要件）。検証: 段階が下がる採点をしたあとでも累計が減らないこと
- [x] 2.3 いま持っているものを出題間隔で分類する読み取りを実装する。**間隔は「次回出題日 − 最後に採点した日」で求める**（design.md D4）。検証: 段階ごとに正しい層へ入ることと、**スケジューラの内部状態を読んでいないこと**をテストで確認する
- [x] 2.4 問答を持たないメモが分類に数えられないことをテストする
- [x] 2.5 分類の境目を純関数に切り出してテストする。検証: 各段階がどの層に入るかを固定する。**層の数を変えるのが1箇所で済むこと**（design.md Open Questions）

## 3. 記録タブと画面

`docs/design-decisions.md` の語り口と配色に合わせる。**説明文を足さない**（design.md 制約4）。

- [x] 3.1 下部タブを3つにする（メモ／復習／記録）。検証: 3つとも切り替わり、選択中が分かることをブラウザで確認する
- [x] 3.2 累計を示す。検証: 「N回 思い出しました」が出て、復習すると増えることをブラウザで確認する
- [x] 3.3 層を示す。検証: 段階の違う問答を用意し、それぞれの層に数えられることをブラウザで確認する。**記録の無い既存データを含む状態で確認する**（その場で採点して作ったデータだけだと、重大な誤分類を見逃す）
- [x] 3.4 **何も無いときに責めないことを確認する**（design.md D8）。検証: 記録が無い状態で、促しや煽りの文言が出ていないこと
- [x] 3.5 **達成率・連続記録・順位を出していないことを確認する**（spec の MUST NOT）。検証: 画面に率・連続・順位に類する表示が無いこと
- [x] 3.6 コントラストと当たり判定を実測する。検証: ライト・ダーク両モードで文字 4.5:1 以上、非文字の部品 3:1 以上

## 4. 記録に残す

- [x] 4.1 `docs/design-decisions.md` のタブの行を更新する。**理由を書く**（design.md D1）。検証: なぜ3つにしたか、なぜ元の決定を変えたかが読み取れること
- [x] 4.2 **分母の線引きを書く**（proposal の `## Why`）。検証: 「責めるものには分母がある」という区別が、コピーのトーンと同じ場所に置かれていること

## 5. 仕上げ

- [x] 5.1 spec に書かれたシナリオを一通り辿る。**人が押せる経路で辿る**（.learnings の L10）。実クリックができない環境ならその旨を明示して人に委ねる
- [x] 5.2 既存の振る舞いが落ちていないことを確認する。検証: 復習・通知・タグ・生成・絞り込みが、いままでどおり動くこと
- [x] 5.3 `npx vitest run`、型チェック、`next build`、OpenNext ビルド、cron worker の型チェック、`scripts/scan-secrets.sh` がすべて通ること
- [x] 5.4 本番の D1 にマイグレーションを適用する。**本番に触るので、事前に利用者の確認を取る**
  - **2026-08-25 に適用した。** 確認を取ってから実行。本番に `review_events` ができ、`ON DELETE cascade` も付いていることを確認済み。既存データには触れていない
- [x] 5.5 デプロイして実機で確認する。検証: タブが3つになり、復習すると累計が増えること
  - **2026-08-25 に確認できた。** 実機でタブが3つになっていること、検証環境で画面が期待どおりであることを確認。空状態の文言「まだ、なし」に違和感があるとの指摘を受け、「まだありません」に直した（メモ一覧の「まだ何もありません」とも揃う）。**本番の棒は、まだ復習していないため未確認**
- [ ] 5.6 しばらく使って、**層の数と、累計だけで足りるか**を見る（design.md Open Questions）。**このタスクは人間の判断が要る**
