## 1. タグの選び手

- [x] 1.1 入力欄と候補列を1つの選び手にまとめる。**いま付いているタグの印を押すと開く**（design.md D1）。タグが無いメモには `＋ タグ` の印を出す。検証: 印を押すと探す欄と候補が出て、外に触れると閉じることをブラウザで確認する
- [x] 1.2 打つと候補が絞り込まれる。検証: 一部を打つと一致するものだけが残ること
- [x] 1.3 完全に一致するものが無いときだけ「◯◯」をつくるを出す（design.md D2）。検証: 既存のタグ名を打ち切ると「つくる」が消えることをテストで確認する。**判定は `validateTagName` と同じ正規化を使う**
- [x] 1.4 絞り込みと「つくる」の判定を純関数に切り出してテストする。検証: 前後の空白、完全一致、部分一致、候補が0件の場合を固定する
- [x] 1.5 候補を選ぶとタグが付き、候補が閉じる。検証: 選んだタグが印として出て、一覧にも反映されることをブラウザで確認する
- [x] 1.6 「つくる」で新しいタグが付く。検証: 打った名前のタグができ、以後は候補に並ぶこと
- [x] 1.7 `Enter` でも確定できる（design.md D3）。検証: 完全一致があればそれが選ばれ、無ければ作られること
- [x] 1.8 いま付いているタグを **×** で外せる。**× と、それ以外（開く）の当たり判定を分ける**（design.md D1）。検証: × を押すと外れ、印の本体を押すと選び手が開くこと
- [x] 1.9 **確定のボタンを別に置かない**が、押せる的は必ずある（.learnings L10）。検証: 候補の行と「つくる」の行がボタン要素で、キーボードから到達できること
- [x] 1.10 候補が増えたときのために高さの上限を置く。検証: 候補を多めに用意し、上限で中がスクロールすること

## 2. 削除の確認

- [x] 2.1 確認をシートで出す（design.md D4）。検証: 「消す」を押すとシートが開くことをブラウザで確認する
- [x] 2.2 **起動した場所と実行のボタンが離れていることを実測する**（spec の要件）。検証: 「消す」の位置と、シートの「消す」の位置を座標で比べ、重なっていないことを示す
- [x] 2.3 続けて二度触れても消えないことを確認する。検証: 「消す」の座標を2回続けて押して、メモが残っていること
- [x] 2.4 シートから取り消せる。検証: 「やめる」で閉じ、メモが残ること
- [x] 2.5 取り消せない旨の文はシートの中に置く。検証: 「問と答、復習の記録も一緒に消えます。戻せません」が読めること
- [x] 2.6 削除中はシートを閉じない（design.md D5）。検証: 失敗したときシートの中に出て、押し直せること

## 3. 仕上げ

- [x] 3.1 コントラストと当たり判定を実測する。検証: 候補の行・×・シートのボタンについて、文字 4.5:1 以上、押せる範囲が 44px 相当を下回らないこと
- [x] 3.2 **説明文を増やしていないことを確認する**（design.md 制約3）。検証: 追加した文言を数え、使い方の説明が入っていないこと
- [x] 3.3 既存の振る舞いが落ちていないことを確認する。検証: 付ける・差し替える・外す・絞り込み・削除が、いままでどおり動くこと
- [x] 3.4 `npx vitest run`、型チェック、`next build`、OpenNext ビルド、cron worker の型チェック、`scripts/scan-secrets.sh` がすべて通ること
- [x] 3.5 デプロイして実機で確認する。**指で触る経路は私の環境では踏めない**（.learnings L10）ので、確認できていない範囲を明示して委ねる
  - **2026-08-25 に確認できた。** 実機で問題なしとの報告。私の側で確かめられていなかった「× と印の押し分け」も、指で自然に働いた
