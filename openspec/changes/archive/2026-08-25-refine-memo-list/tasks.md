## 1. メモ詳細を全画面にする

- [x] 1.1 詳細を全画面の部品として作り直す（design.md D1）。通知の設定と同じく、上に「戻る」を置き、下部タブは残す。検証: 一覧からメモを選ぶと全画面で開き、「戻る」で一覧に返ることをブラウザで確認する
- [x] 1.2 詳細が指すメモを id で保持する（design.md D7）。検証: タグで絞り込んだ状態で詳細を開き、そのタグを外しても画面が閉じないことをブラウザで確認する
- [x] 1.3 詳細に問と次回の出題日を出す。検証: 問と答を持つメモの詳細に、問の文言と次回出題日が出ること
- [x] 1.4 **詳細に答えが出ないことを確認する**（design.md D5）。検証: 画面にも `/api/memos` の応答にも答えが含まれないこと
- [x] 1.5 つくり直すと、未作成のメモへの作成導線を詳細へ移す。検証: どちらも詳細から実行でき、一覧には出ていないことをブラウザで確認する
- [x] 1.6 シートの部品を消す。検証: 使われなくなった部品がリポジトリに残っていないこと

## 2. 一覧の行を削る

- [x] 2.1 行に出すものを本文・タグ・状態の印だけにする（design.md D2）。検証: 問の文言と次回出題日が一覧に現れないことをブラウザで確認する
- [x] 2.2 行のどこを押しても詳細が開くようにし、「くわしく」を無くす。検証: 本文・タグ・余白のいずれを押しても開くこと。**キーボードでも到達できること**
- [x] 2.3 一覧に取り消せない操作が無いことを確認する。検証: 行に削除の導線が無いこと

## 3. 状態の印

- [x] 3.1 3つの状態を形で描き分ける（design.md D3）。検証: 3状態が混ざった一覧で、色を無くしても見分けられること
- [x] 3.2 印に読み上げ用の説明を付ける。検証: それぞれの印に状態を表す名前が付いていること
- [x] 3.3 作成中の点滅を `prefers-reduced-motion` で止める。検証: 設定を有効にすると点滅しないこと
- [x] 3.4 コントラストを実測する。検証: 印・本文・タグについて、ライト・ダーク両モードで文字 4.5:1 以上、非文字の部品 3:1 以上であることを計算値で示す

## 4. 仕上げ

- [x] 4.1 spec に書かれたシナリオを一通り辿る。**人が押せる経路で辿る**（.learnings の L10）。自分の環境で実クリックができない場合は、その旨を明示して人に委ねる
  - **この環境ではブラウザ枠の実クリックが応答しない**（`computer` がタイムアウトする）。要素そのものへの click と、打鍵と同じ経路での入力までは通したが、**指で触る経路は踏めていない**。実機での確認は 4.4 で人に委ねる
- [x] 4.2 既存の振る舞いが落ちていないことを確認する。検証: 絞り込み・タグの付け外し・削除・作り直し・問と答の手動作成・提案の帯が、いままでどおり動くこと
- [x] 4.3 `npx vitest run`、型チェック、`next build`、OpenNext ビルド、cron worker の型チェック、`scripts/scan-secrets.sh` がすべて通ること
- [x] 4.4 デプロイして実機で確認する。検証: 一覧が読みやすくなったか、詳細に必要なものが揃っているか
  - **2026-08-25 に確認できた。** 実機で問題なしとの報告。4.1 で踏めていなかった「指で触る経路」もここで埋まった
