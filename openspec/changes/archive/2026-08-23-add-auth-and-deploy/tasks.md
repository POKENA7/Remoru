## 1. Clerk の導入

- [x] 1.1 Clerk のアプリケーションを作成し、公開サインアップを有効にする。取得した鍵を `.dev.vars` に入れる。**リポジトリに入れないこと**。検証: `git status` に `.dev.vars` が現れないこと、`git check-ignore .dev.vars` が一致を返すこと
- [x] 1.2 `.dev.vars.example` にプレースホルダだけを置く。検証: 実際の鍵らしき文字列が含まれていないこと
- [x] 1.3 Clerk の SDK を導入し、アプリを Provider で包む。検証: `npm run dev` でトップページが 500 にならずに表示されること
- [x] 1.4 サインイン・サインアップの画面を用意する。検証: 未認証でトップに来るとサインインへ誘導され、そこからアカウントを作ってサインインできることをブラウザで確認する
- [x] 1.5 サインアウトの導線を置く。検証: サインアウトすると未認証の状態に戻り、メモが見えなくなることをブラウザで確認する

## 2. 利用者の識別

design.md D1・D2 に対応する。

- [x] 2.1 `getCurrentUserId()` を1つだけ用意する。Clerk を知るのはこの関数だけにする。検証: ドメイン層（`lib/memos.ts` / `lib/quiz-items.ts` / `lib/review.ts`）が Clerk を import していないことをテストで確認する
- [x] 2.2 API のルートが `SINGLE_USER_ID` ではなく `getCurrentUserId()` の値を渡すようにする。検証: 全ルートで差し替わっていること、`SINGLE_USER_ID` の参照が残っていないことを検索で確認する
- [x] 2.3 `SINGLE_USER_ID` を削除する。検証: 定義も参照も残っていないこと、テストが通ること
- [x] 2.4 ルートが要求由来の値を `userId` に渡していないことを検査で固定する。検証: ソースを走査し、要求の本文・問い合わせ文字列・経路から得た値が `userId` に渡っていないことを確かめるテストが通ること。**わざと違反を入れて落ちることも確認する**

## 3. 未認証の遮断

- [x] 3.1 middleware で、認証画面と静的資産以外を認証必須にする。検証: 未認証で `/api/memos` を叩くと拒否されることを確認する
- [x] 3.2 middleware の除外リストが認証画面と静的資産だけであることを検査で固定する。検証: `/api/` 配下を丸ごと公開扱いにする記述が無いことを確かめるテストが通ること（design.md D3）
- [x] 3.3 未認証で操作を要求したときの挙動を確認する。検証: メモの保存と自己採点の記録が、未認証では実行されないことを確認する

## 4. 利用者ごとの分離

- [x] 4.1 メモの分離をテストする。検証: 2人分のデータを作り、一覧・削除のいずれでも他方に到達できないことを確認する
- [x] 4.2 問答の分離をテストする。検証: 他人のメモに問答を作れないこと、未作成の件数に他人の分が混ざらないことを確認する
- [x] 4.3 復習の分離をテストする。検証: 出題対象に他人の問答が混ざらないこと、他人の問答を採点しても相手の次回出題日が変わらないことを確認する

## 5. PWA シェル

- [x] 5.1 manifest とアイコンを追加する。**Service Worker は入れない**（design.md D7、change 4 で入れる）。検証: ブラウザが manifest を読み込めていること、`git status` に Service Worker のファイルが現れないこと
- [x] 5.2 ホーム画面に追加したときの見え方を確認する。検証: 名前とアイコンが意図どおりに出ること

## 6. デプロイ

- [x] 6.1 `npm run deploy` がテスト・型チェック・ビルドを順に実行し、どれかが落ちたら出荷しないようにする。検証: **わざとテストを1つ落とした状態で `npm run deploy` を実行し、デプロイに到達しないことを確認する**（design.md D6）
- [x] 6.2 本番の D1 を作成し、`wrangler.jsonc` に識別子を反映してマイグレーションを適用する。検証: 本番のデータベースに3つのテーブルが存在することを `wrangler d1 execute --remote` で確認する
- [x] 6.3 本番のシークレットを登録する。検証: `wrangler secret list` に必要な鍵が並ぶこと。値がリポジトリに無いこと
- [x] 6.4 デプロイする。検証: workers.dev の URL が 200 を返すこと
- [x] 6.5 **本番の URL でサインインし、メモの投入から復習までを実際に通す。** 検証: アカウントを作り、メモを書き、問と答を作り、期日を迎えた出題に答えられること。**先行実装が越えられなかった線はここ**であり、ここを通るまでこの change は完了しない
- [x] 6.6 デプロイ手順を文書に残す。検証: 手順だけを見て次回のデプロイができる程度に書かれていること

## 7. 仕上げ

- [x] 7.1 spec に書かれたシナリオをブラウザで一通り辿る。検証: 4つの capability のシナリオが期待どおりに振る舞うこと（L05）
- [x] 7.2 鍵が1つもリポジトリに入っていないことを確認する。検証: 全履歴を対象に鍵らしき文字列を検索し、一致が無いこと。**public リポジトリなので、入っていたら push 済みの時点で漏洩**であり、削除ではなく再発行が必要になる
- [x] 7.3 `npx vitest run`、型チェック、`next build`、OpenNext ビルドがすべて通ることを確認する
- [x] 7.4 実際に数日、自分のスマホから使ってみて気づいたことを書き留める
  - **注記**: この評価は未実施のまま change を閉じた（2026-08-23）。実機で一周できることは 6.5 で確認済みだが、「日常に入るか」の判断はまだ得られていない。change 4（通知）の設計は、この判断が無い前提で進める。（日常に入るか／通知が無い状態で開きに行くか／サインインの手間が障害にならないか）。**このタスクは人間にしか実行できない**
