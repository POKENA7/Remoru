## 1. 書き直しの保存先

- [x] 1.1 `PUT /api/memos/[memoId]/quiz-item` を、AI での作り直しから**問と答を受け取る保存**に変える（design.md D5）。検証: 他人のメモを書き直せないことをテストで確かめる。持ち主の絞り込みを外すと赤くなることを確かめてから採用する
- [x] 1.2 手で作成するときと同じ検証を通す（空でない・長さの上限）。検証: 空の問／空の答／上限超えの3通りが弾かれることをテストで確かめる
- [x] 1.3 **`review_schedules` に触れない**（spec「書き直しは復習の進みを変えない」）。検証: 何度か採点して段階を進めたあと書き直し、次回出題日と段階が変わらないことをテストで確かめる。更新を1行足すと赤くなることを確かめてから採用する
- [x] 1.4 `lib/quiz-generation-run.ts` の `replaceExisting` を消す。検証: 型検査と既存のテストが通ることを確かめる

## 2. シートを書き直しにも使う

- [x] 2.1 `QuizSheet` が初期値（いまの問と答）と保存先（POST / PUT）を受け取れるようにする（design.md D6）。検証: 初期値ありで開くと欄が埋まっていることを確かめる
- [x] 2.2 空欄から始めない（spec「いまの内容から直す」）。検証: 初期値を渡したのに空で開くと赤くなる検査を置く
- [x] 2.3 失敗しても入力が残る挙動を保つ。検証: 既存の作成経路が今までどおり動くことをブラウザで確かめる

## 3. 詳細画面

- [x] 3.1 答えを表示する（design.md D1）。検証: 問と答が並んで出ることをブラウザで確かめる
- [x] 3.2 並べ方を K にする。問はオレンジの版、答は青の版、日付には版を当てない（design.md D2）。検証: 3つの左線の色を実測して確かめる
- [x] 3.3 鉛筆を「復習」の見出しの隣に置く。印だけ、読み上げ名は「問と答を書き直す」（design.md D4）。検証: 的が 44px 以上であることと、読み上げ名が残っていることを確かめる
- [x] 3.4 `.redo` と `.detail-foot` を消し、「消す」を上の帯へ移す（design.md D3）。検証: 的が 44px 以上、色が `--danger`、`.detail-foot` がどこにも残っていないことを確かめる
- [x] 3.5 「メモ」の見出しを落とす。検証: 戻る矢印が残っていて、読み上げ名が「戻る」のままであることを確かめる

## 4. 色と的の検査

- [x] 4.1 詳細画面の押せるものが全て 44px 以上であることを検査する。検証: `.redo` のような的の小さいものを1つ足すと赤くなることを確かめてから採用する
- [x] 4.2 コントラストを実測する。検証: 消す・鉛筆・問・答をライトとダークの両方で測り、文字 4.5:1・非文字 3:1 を満たすことを確かめる（学び L03: 配色の切替は CDP で行う）

## 5. 画面で辿る

- [x] 5.1 spec のシナリオを1つずつブラウザで辿る（学び L05）。開く → 答えが見える → 鉛筆 → 直す → 保存 → 次回出題日が変わっていない、の順
- [x] 5.2 やめる・空で保存・失敗、の3経路を辿る。検証: それぞれ以前の問答が残ることを確かめる
- [x] 5.3 押せる経路で確かめる（学び L10）。鉛筆・やめる・直すは要素そのものを `.click()` で動かし、入力はネイティブ setter で入れた。**実際の指のポインタ操作は踏めていない**（この環境では実クリックが効かない）ので 5.4 で実機に委ねる
- [ ] 5.4 デプロイして実機で確かめる（**このタスクは人間の確認が要る**）
