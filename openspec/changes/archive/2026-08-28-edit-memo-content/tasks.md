## 1. 本文の書き換え

- [x] 1.1 `lib/memos.ts` に本文を書き換える関数を足す（design.md D5）。検証: 他人のメモを書き換えられないことをテストで確かめる。持ち主の絞り込みを外すと赤くなることを確かめてから採用する
- [x] 1.2 投入時と同じ検証を通す。検証: 空白のみ・上限超えの2通りが弾かれ、以前の本文が残ることをテストで確かめる
- [x] 1.3 **`quiz_items` にも `review_schedules` にも触れない**。検証: 段階を進めたあと本文を書き直し、次回出題日と段階が変わらないことをテストで確かめる。予定の更新を1行足すと赤くなることを確かめてから採用する
- [x] 1.4 `PUT /api/memos/[memoId]` を足す。検証: 未認証で 401 になることを確かめる（境界検査が全ルートを走査する）

## 2. シートに本文を足す

- [x] 2.1 `QuizSheet` が本文の欄を扱えるようにする。検証: 本文の初期値が入って開くことを確かめる
- [x] 2.2 問答が無いメモでは本文だけを出す（design.md D3）。検証: 問答なしで開いたとき問と答の欄が出ないことをテストで確かめる
- [x] 2.3 保存は本文 → 問答の順で、**変更が無い側は書かない**（design.md D4）。検証: 本文だけ直したとき問答への要求が飛ばないことをテストで確かめる。順序を入れ替えると赤くなる検査を置く

## 3. 詳細画面

- [x] 3.1 鉛筆を上の帯へ移し、復習の見出しの隣からは外す（design.md D2）。検証: 画面の鉛筆が1つであることと、的が 44px 以上であることを確かめる
- [x] 3.2 読み上げ名を「このメモを書き直す」にする。検証: 印だけのボタンに読み上げ名が残っていることを確かめる
- [x] 3.3 上の帯の2つが見分けられることを確かめる。検証: 鉛筆と消すの色を実測し、別の色であることと、それぞれが基準を満たすことを確かめる（学び L03）

## 4. 画面で辿る

- [x] 4.1 spec のシナリオを1つずつブラウザで辿る（学び L05）。開く → 鉛筆 → 本文を直す → 保存 → 一覧に反映 → 次回出題日が変わっていない、の順
- [x] 4.2 本文と答えを一緒に直す経路を辿る。検証: 「火曜定休」→「水曜定休」と直し、答も「水曜」に直せることを確かめる
- [x] 4.3 問答の無いメモで開き、本文だけの形になることを確かめる
- [x] 4.4 押せる経路で確かめる（学び L10）。鉛筆・直す・やめるは要素そのものを `.click()` で動かし、本文はネイティブ setter で入れた。**実際の指のポインタ操作は踏めていない**ので 4.5 で実機に委ねる
- [x] 4.5 デプロイして実機で確かめる。検証: 実機で問題ないことを確認済み
