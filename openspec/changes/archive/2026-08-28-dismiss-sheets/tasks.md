## 1. 閉じ方をまとめた部品

- [x] 1.1 シートの外枠を1つの部品にし、2つのシートの中身を差し替える形にする（design.md D1）。検証: 既存の2つが今までどおり開き、保存とやめるが動くことをブラウザで確かめる
- [x] 1.2 外側に触れたら閉じる。検証: シートの中を押しても閉じないことをテストで確かめる（当たり判定を外側だけに絞る）
- [x] 1.3 Escape で閉じる。検証: シートの中の入力欄で Escape を押しても閉じることを確かめる
- [x] 1.4 **閉じるボタンは残す**（spec「引く操作だけにしない」）。検証: 部品からボタンを外すと赤くなる検査を置く
- [x] 1.5 どの閉じ方でも同じ結果になる。検証: 外側・引く・ボタン・Escape の4通りで、実行されず入力も保存されないことをテストで確かめる

## 2. 引いて閉じる

- [x] 2.1 引いた分だけ追随させ、離したときに閉じるか戻すかを決める（design.md D4）。検証: 距離・速さそれぞれの境界を純関数のテストで確かめる。片方の判定を外すと赤くなることを確かめてから採用する
- [x] 2.2 上へは引けない。検証: 上向きの動きで `translateY` が負にならないことをテストで確かめる
- [x] 2.3 中身が一番上のときだけ引ける（design.md D3、spec「引く操作はスクロールを妨げない」）。検証: `scrollTop > 0` から引き始めたときシートが動かないことをテストで確かめる。判定を外すと赤くなることを確かめてから採用する
- [x] 2.4 掴み手の上からは常に引ける。検証: 中身をスクロールした状態でも掴み手から引けることを確かめる

## 3. 動きと焦点

- [x] 3.1 `prefers-reduced-motion` では追随も戻りも出さない（design.md D5、`motion` の要件）。検証: 動きを止めた状態で、外側・ボタン・Escape が残っていることを確かめる
- [x] 3.2 閉じたあと、開く前の場所へ焦点を戻す（design.md D6）。検証: 鉛筆から開いて閉じたとき、焦点が鉛筆に戻ることをブラウザで確かめる
- [x] 3.3 控えた要素が消えていたときは何もしない。検証: メモを消したあとに焦点の復帰で落ちないことを確かめる

## 4. 画面で辿る

- [x] 4.1 spec のシナリオを1つずつブラウザで辿る（学び L05）。2つのシートそれぞれで、4通りの閉じ方を試す
- [x] 4.2 削除の確認を閉じても消えないことを確かめる（`memo-capture` の新しいシナリオ）
- [x] 4.3 押せる経路で確かめる（学び L10）。外側・中身・Escape・ボタンは要素そのものに送り、**引く操作は PointerEvent の連なりを合成して**踏めた（大きく引く→閉じる／少し引く→戻る、確認シートも同様）。ただし**実際の指の慣性やスクロールとの取り合いは合成では再現できない**ので 4.4 で実機に委ねる
- [x] 4.4 デプロイして実機で確かめる。検証: 実機で問題ないことを確認済み
