## 1. データ層

- [x] 1.1 `memos` に生成の状態を持つ列を足す（design.md D2）。既存の行は未作成として読める値で埋まること。検証: マイグレーションを生成してローカル D1 に適用し、既存のメモが未作成に分類されることを確認する
- [x] 1.2 一覧の状態を「作成済み・作成中・未作成」の3つに分類する読み取りを実装する。検証: 3状態のメモを混ぜて、それぞれが期待どおりに分類されることをテストで確認する
- [x] 1.3 一定時間を過ぎた「作成中」を未作成として扱う（design.md Risks）。検証: 古い作成中のメモが未作成に分類され、手動作成の導線が出る側に入ることをテストで確認する

## 2. 生成の中身（外部に出ない部分）

design.md D4。外部呼び出しと分けて、純粋な部分だけを先に固める。

- [x] 2.1 モデルの応答を検証する純粋関数を書く。検証: 型チェックが通り、その関数が HTTP・D1・フレームワークを import していないこと
- [x] 2.2 壊れた応答を弾くことをテストする。検証: 空文字・長すぎる値・欠けた値・型違いがすべて失敗になり、正常な組だけが通ること
- [x] 2.3 メモの本文から入力を組み立てる関数を書く。検証: **そのメモの本文だけ**が入り、他のメモが混ざらないことをテストで確認する

## 3. 生成の呼び出し

- [x] 3.1 Claude を呼ぶ関数を、呼び先を差し替えられる形で実装する（change 4 の配信と同じ形）。検証: 手書きの偽の呼び先で、成功・失敗・壊れた応答の3通りを扱えることをテストで確認する。**ネットワークに出ないこと**
- [x] 3.2 鍵が無い環境では呼び出さないことをテストする（design.md D7）。検証: 鍵が無いと生成を試みず、メモが未作成になること
- [x] 3.3 モデルの出力を指示として解釈しないことをテストする。検証: 本文に指示めいた文字列を書いても、出力は問と答の2文字列としてしか使われず、長さの上限を超えるものは失敗になること

## 4. 保存したときに生成を起こす

- [x] 4.1 メモの保存が生成を待たないことをテストする（design.md D1）。検証: 生成が終わらなくても保存の応答が返り、メモが一覧に出ること
- [x] 4.2 生成が成功するとメモが復習の対象に入ることをテストする。検証: 問と答が保存され、次回出題日が決まること
- [x] 4.3 生成が失敗してもメモが残ることをテストする。検証: メモが消えず、未作成として分類されること
- [x] 4.4 他人のメモの生成を要求できないことをテストする。検証: 別の利用者のメモに対する生成が行われないこと

## 5. 作り直し

- [x] 5.1 作り直しの受け口を作る。検証: 未認証で 401、他人のメモでは存在しないものとして応答すること
- [x] 5.2 作り直しが**行を置き換えず中身を書き換える**ことをテストする（design.md D5）。検証: 何度か復習を終えたメモを作り直しても、復習の段階と次回出題日が前と同じであること。**削除して作り直す実装にするとこのテストが落ちること**も確認する
- [x] 5.3 作り直しに失敗したとき、以前の問と答が残ることをテストする。検証: 復習の対象からも外れないこと

## 6. 画面

`docs/design-decisions.md` の語り口と配色に合わせる。下部タブは増やさない。

- [x] 6.1 一覧に「作成中」の状態を足す。検証: 3つの状態が本文を読まずに見分けられることをブラウザで確認する
- [x] 6.2 作成中のメモには作成の導線を出さないことを確認する。検証: 作成中の行に手動作成のリンクが無いこと
- [x] 6.3 問答を持つメモに作り直しの導線を出す。検証: 押すと作り直され、一覧に反映されることをブラウザで確認する
- [x] 6.4 「作成中」が残る間だけ、上限を決めて一覧を取り直す（design.md D9）。検証: 生成が終われば表示が変わり、上限に達したら問い合わせが止まること
- [x] 6.5 生成の失敗が警告として現れないことを確認する（design.md D6）。検証: 失敗したメモが未作成として静かに並ぶだけで、赤い表示や謝罪の文言が出ないこと

## 7. デプロイ

- [x] 7.1 `ANTHROPIC_API_KEY` を本体 worker のシークレットに登録する。**このタスクは人間が実行する**。検証: `wrangler secret list` に並ぶこと
- [x] 7.2 本番の D1 にマイグレーションを適用する。検証: 本番の `memos` に列があること
  - **2026-08-25 に適用した。** 本番の `memos` に `quiz_pending_since` があることを `PRAGMA table_info` で確認済み（アーカイブ後の追記）
- [x] 7.3 デプロイして、実機でメモを書いたら問答ができることを確認する。検証: 書いた直後は作成中で、しばらくして復習の対象に入ること
  - **2026-08-25 に確認できた。** 実機で書いたメモから問答ができ、`quiz_pending_since` が落ちて次回出題日が入った。生成された問は次のとおり（アーカイブ後の追記）

        メモ  レトロニムとは、新しいモノや技術が登場したことで、もともと
              あった古い方のモノを区別して呼ぶために、後から新しくつけ
              られた名前のこと
        問    古い方のモノを区別するために後からつけられた名前を何というか？
        答    レトロニム
- [x] 7.4 `docs/deploy.md` に鍵の手順を追記する。検証: 鍵が無いとどう見えるか（生成が起きず未作成になる）が書かれていること

## 8. 仕上げ

- [x] 8.1 spec に書かれたシナリオを一通り辿る。自動で確かめられないものは 7 で確認したことをもって満たすとする
- [x] 8.2 鍵がリポジトリに入っていないことを確認する。検証: `scripts/scan-secrets.sh` を `ANTHROPIC_API_KEY` にも対応させ、偽の鍵を仕込んで落ちることを確認したうえで、全履歴で一致が無いこと
- [x] 8.3 `npx vitest run`、型チェック、`next build`、OpenNext ビルド、cron worker の型チェックがすべて通ること
- [ ] 8.4 実際のメモを何件か通して生成の品質を見て、モデルの選択を見直す（design.md Open Questions）。**このタスクは人間の判断が要る**
  - **未実施のままアーカイブした（2026-08-24）。** 6件見た限り内容は使えるが**文体が揃わない**（敬体と常体、句点の有無が混在）。「図書館の返却期限は何日ですか？」のように問いがやや曖昧になる例もあった。モデルを変えるかプロンプトに文体を足すかは人間の判断
