## 1. 復習まわり（いちばん効く画面）

- [x] 1.1 復習・開始前から説明を3つ削る（「ぜんぶで1分くらい。」「途中でやめても、また出てきます。」「1枚見るたび、ひとマス刷られます」）。検証: 画面に残る文言が「今日は／N枚／はじめる／気が向いたときで／通知の設定」だけになることをブラウザで確認する
- [x] 1.2 **「気が向いたときで」が残っていることを確認する**（design.md D1・確定した言い回し）。検証: 削除後の画面にその文言があること
- [x] 1.3 復習が0件の画面から「出す番の問いがありません。」を削る。検証: 「今日は、なし」と「また溜まったら、ここに出てきます。」が残ること
- [x] 1.4 復習の完了画面と出題中に、確定した言い回しが残っていることを確認する（おしまい／つづきはまた、そのうち。／思い出せなくても大丈夫）

## 2. 一覧・詳細

- [x] 2.1 一覧の空状態から「上の欄に、覚えておきたいことを書いてみてください。」を削る。検証: 「まだ何もありません」だけが残ること
- [x] 2.2 絞り込みが0件のときの「上の「ぜんぶ」に戻すと、すべて表示されます。」を削る。検証: 「このタグのメモはありません」が残り、**解除の手段（「ぜんぶ」）が画面にあること**
- [x] 2.3 入力欄の補助文「ひとことでいい」を削る。検証: プレースホルダ「いま、覚えておきたいこと」が残り、文字数の表示も残ること
- [x] 2.4 メモ詳細から「つけかえると、いまのタグは外れます」を削る。検証: 付け替えると画面のタグが入れ替わることをブラウザで確認する（説明の代わりに結果が見える）
- [x] 2.5 メモ詳細のラベルを短くする（「問と答をつくり直す」→「つくり直す」、「このメモを消す」→「消す」）。検証: 削除の確認文（「問と答、復習の記録も一緒に消えます。戻せません」）は**残っている**こと

## 3. シート・帯・設定

- [x] 3.1 問と答の作成シートの見出しを「問いのかたちにしておく？」に縮める。検証: 「あとで思い出せるように、」が無くなり、問いかけが残ること
- [x] 3.2 タグの提案の帯から「まとめて分けてみることもできます」を削る。検証: 「タグのないメモがN件」とボタンが残ること
- [x] 3.3 **「あとから1件ずつ変えられます」は残す**（design.md D1、利用者の判断）。検証: 承認の画面にその文言があること
- [x] 3.4 通知の設定の「届くのは、その日の復習がある日だけです。無い日は鳴りません。」を「復習がある日だけ届きます」にする
- [x] 3.5 **iOS の但し書きは残す**（design.md 制約2）。検証: ホーム画面に追加しないと届かない旨が読めること

## 4. ラベルの短縮

- [x] 4.1 「戻る」を `←` にする（メモ詳細・通知の設定）。**読み上げ名は「戻る」のまま**（design.md D2）。検証: `aria-label` が「戻る」であることと、キーボードで到達できることを確認する
- [x] 4.2 短縮したラベルのコントラストと当たり判定を確認する。検証: 文字 4.5:1 以上を実測し、`←` の押せる範囲が指で押せる大きさ（44px 相当）を下回らないこと

## 5. 記録

- [x] 5.1 `docs/design-decisions.md` に削る基準を書く（design.md D1 の表と3つの基準）。検証: 次の change でこの判断を辿れる形になっていること
- [x] 5.2 確定リストから「開かなくても、たまっていきます」を外し、**外した理由を残す**（design.md D5）。検証: 一度も実装されていないことと、この change の向きと逆であることが書かれていること

## 6. 仕上げ

- [x] 6.1 削減の結果を数える。検証: 枠の文言と説明文の字数を、変更前（408字／220字）と並べて示す
- [x] 6.2 画面を一通り辿り、**素っ気なくなりすぎていないか**を見る。検証: トーンの文が残っていることを画面ごとに確認する（design.md Risks）
- [x] 6.3 `npx vitest run`、型チェック、`next build`、OpenNext ビルド、cron worker の型チェック、`scripts/scan-secrets.sh` がすべて通ること
- [x] 6.4 デプロイして実機で確認する。検証: 読む量が減ったか、分からなくなった操作が無いか
  - **2026-08-25 に確認できた。** 実機で問題なしとの報告。素っ気なくなりすぎてもいない
