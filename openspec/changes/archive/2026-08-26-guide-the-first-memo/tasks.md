## 1. 導きを終えたかどうかを持つ

- [x] 1.1 `first_run_state`（`user_id` 主キー、`guided_at`）を `db/schema.ts` に足し、マイグレーションを作る（design.md D1）。検証: ローカルに適用して表ができることを確かめる
- [x] 1.2 読み書きを `lib/first-run.ts` に置く。検証: 未了・終了の2状態と、**他の利用者の記録が混ざらない**ことをテストで確かめる。利用者の絞り込みを外すと赤くなることを確かめてから採用する
- [x] 1.3 一覧の取得と同じ経路で導きの状態を返す。検証: 未認証で 401 になることを確かめる

## 2. 文言

- [x] 2.1 `lib/first-run-text.ts` に誘いの固定文と `announcementText(nextReviewAt, now)` を置く（design.md D7）。検証: 明日・N日後の2通りをテストで確かめる
- [x] 2.2 日境界で切り替わることを確かめる。検証: **23時に書いた直後の告知が「明日」になる**ことをテストで確かめる（時刻の差で割ると1時間→0日になり「このあと」と言ってしまう）。**符号の違うタイムゾーンでも同じ結果になる**ことを確かめる（学び L07）
- [x] 2.3 誘いの文が入力欄の但し書きを繰り返していないこと、**日付も間隔も述べていない**ことを検査する（spec「入力欄と同じことを言わない」「日付を約束しない」）。検証: 文言に「明日」を入れると赤くなることを確かめてから採用する

## 3. 空の一覧はそのままにする（方針変更）

当初は空状態に誘いの一行を置いたが、**書く前の約束は確かめられず、書いた
直後のガイドで足りる**ため取り下げた（design.md D2）。実装したものは戻した。

- [x] 3.1 誘いの実装と文言を取り除き、空の表示を「まだ何もありません」に戻す。検証: 0件でその文が出ることをブラウザで確かめる
- [x] 3.2 誘いに関する検査と spec の要件も落とす。検証: `openspec validate` が通り、`INVITATION` と `invitation()` がどこにも残っていないことを確かめる

## 4. 最初の問答ができたことを告げる

- [x] 4.1 導きが未了の利用者に問答が1つ現れたら、そのメモの行の下に告知を1度だけ出す（design.md D3）。検証: 出す・出さないの判定を純関数に切り出してテストする
- [x] 4.2 告知を出したら導きを終えたものとして記録する。検証: 記録後に再読み込みしても告知が出ないことをブラウザで確かめる
- [x] 4.3 生成が失敗したときは何も出さず、**導きも終えない**（design.md D6、spec「失敗しただけでは導きは終わらない」）。検証: 失敗のあと次のメモで告知が出ることをテストで確かめる
- [x] 4.4 2件目以降では出さない。検証: 既に問答を持つ状態でもう1件書き、告知が出ないことをブラウザで確かめる

## 5. 通知をその場で差し出す

- [x] 5.1 購読の処理を `app/notification-settings.tsx` から呼べる形に切り出す（design.md D4）。検証: 設定画面からの購読が今までどおり動くことをブラウザで確かめる
- [ ] 5.2 「いいよ」を押したときに**端末の許可ダイアログが出る**ことを確かめる（**このタスクは実機が要る**）。既に購読がある端末では既存の購読が再利用されダイアログは出ないので、iOS 側で通知を一度オフにしてから辿る
- [x] 5.3 通知を扱えない端末では差し出しを出さず、告知だけを出す（spec「通知を扱えない端末では差し出さない」）。検証: `PushManager` が無い状態で差し出しが消えて告知が残ることをテストで確かめる
- [x] 5.4 見送っても導きは終わり、この場面が二度と出ないことを確かめる（design.md D5）。検証: 見送ったあと再読み込みで出ないことをブラウザで確かめる
- [x] 5.5 答えたことを一覧の外で持つ（design.md D7）。検証: 「いいよ」のあと復習タブ・記録タブ・メモ詳細のそれぞれから戻って、問いに戻らないことをブラウザで確かめる。答えを告知自身に戻すと赤くなる検査を置く

## 6. 覆した確定事項を書き残す

- [x] 6.1 `docs/design-decisions.md` の「初回だけ表示する形は採らない」に、この change で覆したこととその理由を書き足す（design.md D1）。検証: change 8 の判断が消えず、例外として並んでいることを読み返して確かめる

## 7. 表を作る前から使っている人

- [x] 7.0 既に問答を持つ利用者を導き済みとして埋め戻す（0009）。検証: 30日前のメモに告知が出ることを**再現してから**直し、埋め戻しを消す／時刻を「いま」にする／問答を持たない人まで埋める／NULL の行を埋めない、の4通りで赤くなることを確かめる

## 8. 画面で辿る

- [x] 8.1 spec のシナリオを1つずつブラウザで辿る（学び L05）。0件 → 1件書く → 生成待ち → 告知 → 再読み込み → 全部消す、の順
- [x] 8.2 押せる経路で確かめる（学び L10）。差し出しのボタン・保存・削除・タブは要素そのものを `.click()` で動かし、本文はネイティブ setter + input イベントで入れた。**実際のポインタ操作と、許可が granted のときの購読の最後までは踏めていない**（この環境の `Notification.permission` は denied、VAPID 鍵も無い）。7.4 で実機に委ねる
- [x] 8.3 誘いと告知の文字のコントラストを測る（4.5:1 以上）。検証: ライトとダークの両方で計算値を出す
- [x] 8.4 デプロイして、**まだ導きを終えていない利用者として実機で初回を通す**。検証: 空の一覧 → 書く → 告知「明日、まだ覚えているか尋ねてもいいですか？」＋「いいよ」が iPhone で辿れることを確認済み
