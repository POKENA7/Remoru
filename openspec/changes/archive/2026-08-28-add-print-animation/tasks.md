## 1. いま書いた1件を憶える

- [x] 1.1 保存した直後のメモの id を `app/app-shell.tsx` が持つ（design.md D2）。検証: 下書き・タグの提案・告知の答えと同じ場所にあることを確かめる
- [x] 1.2 一度刷ったら印を消す。検証: 印が残ると2回目の unmount でまた刷られるので、**消し忘れると赤くなる**テストを置く
- [x] 1.3 `app/unmount-boundary.test.ts` に4件目として足す。検証: 印を `MemoTab` 側の `useState` に移すと赤くなることを確かめてから採用する

## 2. 刷りの動き

- [x] 2.1 高さを実測してから開く（design.md D1）。検証: 1行の本文と、折り返す長い本文の両方で、開ききった高さが自然な高さと一致することをテストで確かめる
- [x] 2.2 高さ 200ms → インク 420ms（60ms 遅らせ）の二段にする。検証: 尺が本文の長さで変わらないことを確かめる（変わるとリズムが揺れる）
- [x] 2.3 `mix-blend-mode` をダークで `screen` に反転させる。検証: 明暗それぞれでインクの層の色を実測し、地に沈んでいないことを確かめる
- [x] 2.4 波紋を `pointerdown` の座標から出す（design.md D5）。検証: ボタンの左端と右端を押して、出る位置が変わることを確かめる

## 3. 動きを止めた人

- [x] 3.1 `prefers-reduced-motion` で動きを全部止め、橙の版を1本残して引く（design.md D4）。検証: 動きを止めた状態で「加わったこと」が分かることを画面で確かめる
- [x] 3.2 止めたときに何も残らない形になっていないことを検査する（`motion` の要件、学び L06）。検証: 版を残す指定を外すと赤くなることを確かめてから採用する

## 4. 演出の対象を間違えない

- [x] 4.1 描き直された行を刷らない（spec「描き直しでは演出しない」）。検証: タブを往復して戻ったとき、既にある行が刷られないことをブラウザで確かめる
- [x] 4.2 生成中の取り直し（最大8回）でも刷り直されないことを確かめる。検証: 生成の完了を待つ間、行が繰り返し刷られないことをブラウザで確かめる

## 5. 続けて書く

- [x] 5.1 演出の完了を待たずに次を受け付ける（design.md D3）。検証: 動いている間に次を保存できることをブラウザで確かめる
- [x] 5.2 続けて3件書いても表示が崩れない。検証: 人が打つ速さ（約260ms 間隔）で3件入れ、高さが0のまま残る行が無いことを確かめる

## 6. 画面で辿る

- [x] 6.1 spec のシナリオを1つずつブラウザで辿る（学び L05）
- [x] 6.2 押せる経路で確かめる（学び L10）。波紋はボタン要素に `pointerdown` を送り、押した座標（左端 12px）から出ることを確認。本文はネイティブ setter + input イベント。**実際の指のポインタ操作は踏めていない**（この環境では実クリックが効かない）ので 6.3 で実機に委ねる
- [x] 6.3 デプロイして、**実機で10回続けて書く**。検証: 実機で確認し、尺・濃度とも調整不要と判断された
