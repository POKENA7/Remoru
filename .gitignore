.DS_Store
.worktrees/
.superpowers/
node_modules/
.wrangler/
.next/
.open-next/
*.tsbuildinfo

# secrets — このリポジトリは public
.env
.env.*
.dev.vars
.dev.vars.*
!*.example

# 注入テストが作る一時ファイル（scripts/harness/checks.test.ts）
harness-tmp/

# レビューの受領書と、棚卸を一度止めた印。差分に紐づく使い捨てなので履歴に要らない
.harness/*
# ただし棚卸の決定は記録として残す（D9。skip が並ぶかどうかで しきい値 を見直す）
!.harness/promotions.json
